# 安全说明

- 若发现安全问题，请通过 GitHub **Security → Private vulnerability reporting** 联系维护者。  
- **不要** 在 issue 中粘贴账号、Token、生产数据库连接串。  
- 自建部署时请使用强 `NEXTAUTH_SECRET`、强数据库密码，并限制管理后台 `ADMIN_PASSWORD` 与访问来源（如内网、反向代理鉴权）。
